part of 'theme_manager.dart';

const primaryColor = Color(0xFF067A80);

final lightColorScheme = ColorScheme.fromSeed(
  seedColor: primaryColor,
);

final darkColorScheme = ColorScheme.fromSeed(
  seedColor: primaryColor,
  brightness: Brightness.dark,
);
